[pytest]
addopts = -m "not integration and not slow and not pbr"
markers =
    integration: requires a complete 44/88-frame validation dataset
    slow: runs a multi-profile, multi-seed validation matrix
    pbr: requires Blender/Cycles or another external renderer
